# Dependencies
node_modules/

# Build output (regenerated by `pnpm run build` / each package's `dev` script)
dist/
*.tsbuildinfo

# Runtime data (regenerated on demand by the api-server)
artifacts/api-server/uploads/*
artifacts/api-server/cache/*
!artifacts/api-server/uploads/.gitkeep
!artifacts/api-server/cache/.gitkeep

# Env
.env
.env.local

# OS/editor junk
.DS_Store
__MACOSX/
